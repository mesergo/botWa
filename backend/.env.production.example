# Production Environment Variables
NODE_ENV=production

# MongoDB Connection
#---------$$$ MONGODB_URI=mongodb://flowbot:f4l88owb0t@103.95.119.188:27017/flowbot?authSource=flowbot
MONGODB_URI=mongodb://bots:b0t5bots@103.95.119.188:27017/bots?authSource=bots

# Server Configuration  
PORT=3001

# CORS Configuration
CORS_ORIGIN=http://103.95.119.188

# Public URL of the frontend, used in emails (e.g. rep invite links)
SYSTEM_URL=https://app.message.co.il/

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# WhatsApp API endpoint for outbound agent messages
WHATSAPP_ENDPOINT=dialog360/65aec7ebf1a1d64f29645fd9

# Optional: dedicated WhatsApp endpoint for sending phone-login OTP codes.
# Falls back to WHATSAPP_ENDPOINT above if unset.
# AUTH_WHATSAPP_ENDPOINT=dialog360/your-auth-bot-id

# Shared secret required in the x-status-token header for POST /api/360/status
# (called by the external dialog360.js project to forward WhatsApp delivery-status
# webhooks — sent/delivered/read/failed — for messages we pushed out).
WA_STATUS_WEBHOOK_SECRET=replace-with-a-long-random-secret

# Optional: MongoDB Atlas (if using cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bots?retryWrites=true&w=majority

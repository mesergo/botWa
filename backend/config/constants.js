/**
 * System-wide constants shared across backend controllers.
 */

/** Keyword that resets the bot session from any point in the conversation (case-insensitive). */
export const SYSTEM_RESTART_KEYWORD = 'wastart';
